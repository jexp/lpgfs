# LPGFS Progress Log

## Task 001: Create project structure & dependencies
- **Status:** Completed
- **PRD Reference:** Section 12.5 (Implementation Stack)

### Key Decisions
- Used Node.js runtime with ESM modules
- fuse-native moved to optionalDependencies (requires native FUSE libraries on target system)
- TypeScript configured for NodeNext module resolution

### Files Changed
- package.json: Added dependencies (neo4j-driver, yaml, lru-cache, fuse-native as optional), @types/node
- tsconfig.json: Configured for Node.js ESM output with strict mode
- src/types/index.ts: Created placeholder types file
- src/index.ts: Updated module entry point
- Created directory structure: src/{core,fuse,db,cache,config,types}/

### Blockers/Notes
- fuse-native requires FUSE libraries installed on target system (libfuse-dev on Linux, macFUSE on macOS)
- Native compilation needs C compiler and node-gyp build tools

## Task 002: Define core TypeScript types
- **Status:** Completed
- **PRD Reference:** Sections 5.3 (Naming Configuration), 12.3 (Path Resolution), 12.4 (Caching), 12.7 (Mount Options), 12.8 (Error Handling)

### Key Decisions
- Defined PathContext with all fields from section 12.3, plus isConfigFile for /.lpgfs.yaml
- ConfigSchema supports naming overrides per-label and per-relationship-type
- Added DEFAULT_CONFIG and DEFAULT_CACHE_TTL constants for convenience
- Included query result types (NodeQueryResult, RelationshipQueryResult) for DB layer
- Added POSIX error codes as constants with LpgfsError class
- PropertyValue type supports recursive structures (objects/arrays)

### Files Changed
- src/types/index.ts: Complete type definitions (PathContext, ConfigSchema, DatabaseNode, DatabaseRelationship, CacheEntry, MountOptions, etc.)

### Blockers/Notes
- None - all types defined, typecheck passes

## Task 003: Implement .lpgfs.yaml parser
- **Status:** Completed
- **PRD Reference:** Section 5.3 (Naming Configuration)

### Key Decisions
- Created ConfigParser class with static methods for flexibility (no instantiation needed)
- Implemented both `load()` (throws if file missing) and `loadOrDefault()` (returns defaults if missing)
- Validation is thorough with specific error messages for each config section
- User config is merged with defaults, allowing partial config files
- Added `toYaml()` method for serializing config back to YAML (useful for /.lpgfs.yaml endpoint)

### Files Changed
- src/config/parser.ts: New ConfigParser class with load, parse, validate, and toYaml methods

### Blockers/Notes
- None - typecheck passes, implementation complete

## Task 004: Implement filename sanitization
- **Status:** Completed
- **PRD Reference:** Section 8.4 (Property Value Sanitization)

### Key Decisions
- sanitize() function accepts PropertyValue and optional SanitizationConfig
- Uses DEFAULT_CONFIG.sanitization when no config provided
- Handles null/undefined/empty values with '_empty_' placeholder
- Arrays and objects are JSON-stringified (fallback for complex values)
- Trims leading/trailing dots and whitespace
- Added sanitizeElementId() helper for Neo4j elementIds (contain colons)
- Added isValidFilename() validation function

### Files Changed
- src/config/sanitize.ts: New file with sanitize(), sanitizeElementId(), isValidFilename()

### Blockers/Notes
- None - typecheck passes

## Task 005: Implement collision handling
- **Status:** Completed
- **PRD Reference:** Section 5.3 (Collision Handling)

### Key Decisions
- Created `CollisionResolver` class to track used names per context (label/reltype)
- Implements `suffix_elementId` strategy: appends `_<sanitizedElementId>` to colliding names
- Implements `fail` strategy: throws `CollisionError` with descriptive message
- Uses `sanitizeElementId()` from sanitize.ts to handle Neo4j elementId format
- Added `resolveCollisions()` convenience function for batch processing
- Added `createCollisionResolver()` factory for incremental resolution

### Files Changed
- src/config/collision.ts: New file with CollisionResolver class, CollisionError, and helper functions

### Blockers/Notes
- None - typecheck passes

## Task 006: Implement Neo4j connection pool
- **Status:** Completed
- **PRD Reference:** Section 12.5 (Implementation Stack)

### Key Decisions
- Created `DatabaseConnection` class wrapping neo4j-driver
- Neo4j driver handles connection pooling internally (configured maxConnectionPoolSize: 50)
- Implemented retry logic with exponential backoff (default 3 retries)
- `executeQuery()` returns transformed records (Neo4j integers → JS numbers, Nodes/Relationships → plain objects)
- Connection errors mapped to POSIX codes: ServiceUnavailable → ENOENT, others → EIO
- Added `connect()` and `createConnection()` factory functions

### Files Changed
- src/db/connection.ts: New DatabaseConnection class with retry logic and value transformation

### Blockers/Notes
- None - typecheck passes

## Task 007: Implement query: list all labels
- **Status:** Completed
- **PRD Reference:** Section 12.2 (Query Mapping)

### Key Decisions
- Created `getLabels(db)` function in src/db/queries.ts
- Uses Cypher: `CALL db.labels() YIELD label RETURN label`
- Returns string[] (empty array for empty database)
- Takes DatabaseConnection as parameter for dependency injection

### Files Changed
- src/db/queries.ts: New file with getLabels() query function

### Blockers/Notes
- None - typecheck passes

## Task 008: Implement query: list nodes by label
- **Status:** Completed
- **PRD Reference:** Section 5.3 (Naming Strategy), Section 12.2 (Query Mapping)

### Key Decisions
- Created `getNodesByLabel(db, label, config)` function in src/db/queries.ts
- Uses Cypher: `MATCH (n:\`Label\`) RETURN elementId(n), properties(n)` (backticks escape label name)
- Returns `NodeQueryResult[]` with name, elementId, and properties
- Supports naming strategies: 'elementId' (default) or 'property' (via per-label override)
- Missing property falls back to sanitized elementId for robustness
- Integrates with `resolveCollisions()` for collision handling
- Added helper functions `getNamingStrategy()` and `getPropertyName()` for config lookup

### Files Changed
- src/db/queries.ts: Added getNodesByLabel() function with imports for types, sanitize, and collision modules

### Blockers/Notes
- None - typecheck passes

## Task 009: Implement query: get node properties
- **Status:** Completed
- **PRD Reference:** Section 5.1 (Node Properties), Section 12.2 (Query Mapping)

### Key Decisions
- Created `getNodeProperties(db, label, name, config)` function in src/db/queries.ts
- Reuses `getNodesByLabel()` to find node by display name - ensures consistent naming logic
- Returns `NodePropertiesResult` interface: `{ _elementId: string, [key: string]: PropertyValue }`
- Supports both naming strategies (elementId and property) transparently
- Returns `null` if node not found
- Added `NodePropertiesResult` interface for type-safe return

### Files Changed
- src/db/queries.ts: Added getNodeProperties() function and NodePropertiesResult interface

### Blockers/Notes
- None - typecheck passes
- Note: Uses O(n) search over all nodes for label; caching (task-014) will optimize this

## Task 010: Implement query: list relationship types for node
- **Status:** Completed
- **PRD Reference:** Section 12.2 (Query Mapping)

### Key Decisions
- Created `getRelationshipTypes(db, label, name, config)` function in src/db/queries.ts
- Uses Cypher: `MATCH (n)-[r]-() WHERE elementId(n) = $elementId RETURN DISTINCT type(r) AS relType`
- Returns both outgoing and incoming relationship types (combined, as that's what the node dir needs)
- Returns string[] (empty array if node not found or has no relationships)
- Reuses `getNodesByLabel()` to find node by display name - ensures consistent naming logic
- Uses parameterized query with elementId for safety and performance

### Files Changed
- src/db/queries.ts: Added getRelationshipTypes() function

### Blockers/Notes
- None - typecheck passes
- Note: Uses same O(n) lookup pattern as getNodeProperties; caching (task-014) will optimize this

## Task 011: Implement query: list relationships by type & direction
- **Status:** Completed
- **PRD Reference:** Section 5.2 (Relationship Structure), Section 12.2 (Query Mapping)

### Key Decisions
- Created `getRelationships(db, label, name, relType, direction, config)` function in src/db/queries.ts
- Uses Cypher patterns: `(n)-[r:TYPE]->(m)` for OUT, `(n)<-[r:TYPE]-(m)` for IN
- Returns `RelationshipQueryResult[]` with targetName, targetLabel, targetElementId, relElementId, relProperties
- Applies naming strategy to target nodes based on target label (supports cross-label relationships)
- Uses backticks to safely escape relationship type names in Cypher
- Handles collision resolution per target label group
- Returns empty array if source node not found or has no relationships of that type/direction

### Files Changed
- src/db/queries.ts: Added getRelationships() function, imported Direction and RelationshipQueryResult types

### Blockers/Notes
- None - typecheck passes
- Uses same O(n) lookup pattern for source node; caching (task-014) will optimize this
- Target node naming uses first label when node has multiple labels

## Task 012: Implement query: get relationship properties
- **Status:** Completed
- **PRD Reference:** Section 5.2.4 (Relationship Properties)

### Key Decisions
- Created `getRelationshipProperties(db, relElementId)` function in src/db/queries.ts
- Uses Cypher: `MATCH ()-[r]-() WHERE elementId(r) = $relElementId RETURN elementId(r), properties(r) LIMIT 1`
- Returns `RelationshipPropertiesResult` interface: `{ _elementId: string, [key: string]: PropertyValue }`
- Uses parameterized query for safety and performance
- Uses LIMIT 1 since elementId uniquely identifies a relationship
- Returns `null` if relationship not found

### Files Changed
- src/db/queries.ts: Added getRelationshipProperties() function and RelationshipPropertiesResult interface

### Blockers/Notes
- None - typecheck passes
- Straightforward implementation since relationships are looked up by elementId (no naming strategy needed)

## Task 013: Implement in-memory cache with TTL
- **Status:** Completed
- **PRD Reference:** Section 12.4 (Caching)

### Key Decisions
- Created `Cache` class wrapping lru-cache with TTL support
- Uses `ttlAutopurge: true` for automatic expiration cleanup
- TTL determined per-entry based on key prefix (labels/nodes/props/reltypes/rels)
- Added `cacheKey` utility object for building consistent cache keys
- Cache key prefixes: `labels`, `nodes:Label`, `props:elementId`, `reltypes:elementId`, `rels:elementId:relType:direction`
- Used wrapper type `CacheValue = { data: unknown }` to satisfy lru-cache's type constraint

### Files Changed
- src/cache/index.ts: New Cache class with get, set, has, delete, clear methods and cacheKey builder

### Blockers/Notes
- None - typecheck passes
- TTL defaults from DEFAULT_CACHE_TTL: labels=60s, nodes=30s, properties=10s, relationships=10s, symlinks=60s

## Task 014: Integrate cache with database queries
- **Status:** Completed
- **PRD Reference:** Section 12.4 (Caching)

### Key Decisions
- Added optional `cache?: Cache` parameter to all query functions
- Cache is checked before executing database queries (cache-aside pattern)
- Results are stored in cache after successful database queries
- TTL is automatically determined by the cache based on key prefix:
  - `getLabels()` → labels (60s)
  - `getNodesByLabel()` → nodes (30s)
  - `getNodeProperties()` → uses getNodesByLabel cache (30s)
  - `getRelationshipTypes()` → reltypes (10s)
  - `getRelationships()` → rels (10s)
  - `getRelationshipProperties()` → props (10s)
- Cache parameter is optional - functions work without cache for backward compatibility
- Empty results are also cached to avoid repeated queries for non-existent data
- Null results (not found) are cached for relationship properties

### Files Changed
- src/db/queries.ts: Added optional cache parameter to all query functions with cache check/store logic

### Blockers/Notes
- None - typecheck passes
- The cache integration is non-breaking; all functions work without a cache parameter
- Nested functions (like getNodeProperties calling getNodesByLabel) pass the cache down for consistent caching

## Task 015: Implement path parser
- **Status:** Completed
- **PRD Reference:** Section 12.3 (Path Resolution)

### Key Decisions
- Created `parsePath(path)` function that returns `PathContext` object
- Parses all path types: root, label, node, reltype, direction, target, properties
- Detects special files: `.lpgfs.yaml` (config), `.properties.json` (node props), `.targetName.json` (rel props)
- Added `extractTargetFromRelPropertiesFilename()` helper for extracting target names from `.james.json` style filenames
- Added `pathContextToCacheKey()` helper for generating cache keys from PathContext
- Exported constants: `CONFIG_FILENAME`, `PROPERTIES_FILENAME`
- Handles edge cases: trailing slashes, empty paths, self-referential relationships, multiple relationship suffixes
- Added vitest as test framework with comprehensive test suite (37 tests)

### Files Changed
- src/core/path-parser.ts (new): Path parser implementation with helper functions
- src/core/path-parser.test.ts (new): Comprehensive test suite
- package.json: Added vitest, updated test script

### Blockers/Notes
- None - typecheck and all tests pass
- Parser is defensive and handles invalid directions by returning them as-is (FUSE layer handles validation)
- Deep paths beyond target are handled for symlink traversal scenarios

## Task 016: Implement FUSE readdir() for root
- **Status:** Completed
- **PRD Reference:** Section 12.2 (Query Mapping)

### Key Decisions
- Created `src/fuse/handlers.ts` as the FUSE operations module
- Implemented `readdir()` function that dispatches based on PathContext type
- Created `HandlerContext` interface to hold shared resources (db, config, cache, debug)
- `createHandlerContext()` factory function sets up context with defaults
- `readdirRoot()` returns all labels as directories plus `.lpgfs.yaml` file
- Uses existing `getLabels()` query with caching
- Added `getConfigContent()` helper for reading /.lpgfs.yaml (used in future tasks)
- Comprehensive test suite with 9 tests covering labels, config file, empty db, caching

### Files Changed
- src/fuse/handlers.ts (new): FUSE handler implementations with readdir for root
- src/fuse/handlers.test.ts (new): Test suite for handlers

### Blockers/Notes
- None - typecheck and all 46 tests pass
- Prepared structure for subsequent readdir implementations (label, node, reltype, direction)

## Task 017: Implement FUSE readdir() for label directory
- **Status:** Completed
- **PRD Reference:** Section 12.2 (Query Mapping)

### Key Decisions
- Added `readdirLabel()` function to handle `/Label` paths
- Returns all nodes of the given label as directory entries
- Uses existing `getNodesByLabel()` query with caching
- Respects naming config (elementId or property strategy)
- Collision handling applied automatically via `getNodesByLabel()`
- Added 6 new tests covering: node listing, naming strategies, empty labels, trailing slash, collisions

### Files Changed
- src/fuse/handlers.ts: Added case for 'label' type, added readdirLabel() function
- src/fuse/handlers.test.ts: Added label directory test suite with 6 tests

### Blockers/Notes
- None - typecheck and all 52 tests pass
- Straightforward implementation leveraging existing query function

## Task 018: Implement FUSE readdir() for node directory
- **Status:** Completed
- **PRD Reference:** Section 4.1 (Node Directory Structure)

### Key Decisions
- Added `readdirNode()` function to handle `/Label/nodeName` paths
- Returns `.properties.json` file plus all relationship type directories
- Uses existing `getRelationshipTypes()` query with caching
- Properties file is always included (even for non-existent nodes, FUSE layer handles ENOENT)
- Respects naming config when looking up node by display name
- Added 7 new tests covering: rel types listing, no relationships, trailing slash, property naming, non-existent node, multiple rel types

### Files Changed
- src/fuse/handlers.ts: Added import for `getRelationshipTypes` and `PROPERTIES_FILENAME`, added case for 'node' type, added `readdirNode()` function
- src/fuse/handlers.test.ts: Extended mock to handle relationship type queries, added node directory test suite with 7 tests

### Blockers/Notes
- None - typecheck and all 58 tests pass
- Implementation follows same pattern as readdirLabel, leveraging existing query function

## Task 019: Implement FUSE readdir() for reltype directory
- **Status:** Completed
- **PRD Reference:** Section 5.2.2 (Direction Structure)

### Key Decisions
- Added `readdirReltype()` function to handle `/Label/nodeName/RELTYPE` paths
- Always returns `['OUT', 'IN']` as directories (both directions always available)
- Function is synchronous (no database query needed - directions are static)
- Validation of whether the relationship type exists happens in getattr(), not readdir()
- Added 5 new tests covering: OUT/IN directories, any reltype, trailing slash, property naming, non-existent reltype

### Files Changed
- src/fuse/handlers.ts: Added case for 'reltype' type, added `readdirReltype()` function
- src/fuse/handlers.test.ts: Added reltype directory test suite with 5 tests

### Blockers/Notes
- None - typecheck and all 63 tests pass
- Simplest implementation so far - no database queries, just static directory list

## Task 020: Implement FUSE readdir() for direction directory
- **Status:** Completed
- **PRD Reference:** Section 5.2.3 (Symlinks), Section 8.1 (Multiple Relationships)

### Key Decisions
- Added `readdirDirection()` function to handle `/Label/nodeName/RELTYPE/OUT` and `/Label/nodeName/RELTYPE/IN` paths
- Uses existing `getRelationships()` query to fetch target nodes and relationship data
- Returns symlinks to target nodes + `.targetName.json` property files for each relationship
- Handles multiple relationships to same target using suffix pattern: first occurrence uses base name, subsequent get `_1`, `_2`, etc. (per section 8.1)
- Symlinks returned with `type: 'symlink'`, property files with `type: 'file'`
- Target naming respects target label's naming configuration (supports cross-label relationships)
- Added 8 new tests covering: basic symlinks/files, empty results, multiple targets, multiple rels to same target (suffix handling), property naming, IN direction, trailing slash, cross-label relationships

### Files Changed
- src/fuse/handlers.ts: Added import for `getRelationships` and `Direction`, added case for 'direction' type, added `readdirDirection()` function
- src/fuse/handlers.test.ts: Extended mock to handle relationship queries, added direction directory test suite with 8 tests

### Blockers/Notes
- None - typecheck and all 71 tests pass
- Implementation leverages existing `getRelationships()` query function with full caching support
