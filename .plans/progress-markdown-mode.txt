## TASK-001 — 2026-07-22T18:50:12Z
Added mode.type (classic|markdown) and mode.markdown sub-schema (labels, linkStyle, includeIncoming, textProperties, fields) to ConfigSchema, with validation/defaulting in ConfigParser mirroring the existing validateNaming/validateSanitization/validateCollision pattern.
Files: ~src/types/index.ts, ~src/config/parser.ts, +src/config/parser.test.ts
Decisions: mode.markdown defaults are filled in (DEFAULT_MARKDOWN_MODE_CONFIG) whenever mode.type is 'markdown' and the markdown section is omitted; the markdown sub-schema is validated whenever present regardless of mode.type.

## TASK-003 — 2026-07-22T21:00:00Z
Added an optional mode-aware character map to sanitize()/sanitizeElementId() (extends the default replacement map with `[`, `]`, `#`, `^`, `|` when mode is 'markdown') and an optional reservedNames list to CollisionResolver/resolveCollisions/createCollisionResolver so a first-seen name matching a reserved name (e.g. 'index'/'log') is routed through the existing collision strategy.
Files: ~src/config/sanitize.ts, ~src/config/collision.ts, +src/config/sanitize.test.ts, +src/config/collision.test.ts
Decisions: both new parameters are appended positionally with backward-compatible defaults ('classic' mode, no reserved names) so existing callers in src/db/queries.ts are unaffected; reserved names are pre-seeded into the resolver's usedNames map at count 1 with no elementId mapping, so the first real node with that name is treated as a collision rather than a fresh name.

## TASK-005 — 2026-07-22T19:03:37Z
Implemented the pure single-node markdown renderer: frontmatter ordered type -> mapped fields (via a stubbed FieldResolver interface) -> remaining properties (alphabetical) -> per-rel-type link keys (alphabetical, wikilink/markdown styles), plus body composition from configured textProperties (bare/`## heading`/empty).
Files: +src/markdown/renderer.ts, +src/markdown/renderer.test.ts, ~.plans/tasks-markdown-mode.yml
Decisions: FieldResolver is a minimal seam (`resolve(labels, properties, fieldsConfig) -> ResolvedFields`) for task-006's real fallback-list resolver; a literal `type` property is dropped (full clash resolution deferred to task-013); mapped-field key names (title/timestamp/tags) are excluded from "remaining properties" whenever the resolver actually populated them, even if not listed in consumedProperties, to prevent an unrelated same-named property from overwriting a resolved value.

## TASK-002 — 2026-07-22T19:15:54Z
Added a `--mode <classic|markdown>` flag to `lpgfs mount` (validated via a new exported `validateMode`), an optional `mode` field on `MountOptions`, and a `ConfigParser.applyModeOverride(config, cliMode)` static method wired into `Daemon.loadConfig()` so the CLI flag overrides the config file's `mode.type` (filling in default markdown sub-config when switching to markdown) while omitting the flag preserves the file's value.
Files: ~src/cli/index.ts, ~src/types/index.ts, ~src/config/parser.ts, ~src/core/daemon.ts, ~src/cli/index.test.ts, ~src/config/parser.test.ts, ~src/core/daemon.test.ts
Decisions: the override lives in ConfigParser (not daemon.ts) so it's unit-testable without fuse-native/db mocking; daemon.test.ts additionally exercises the private loadConfig() via a cast to confirm end-to-end wiring.

## TASK-018 — 2026-07-22T19:50:30Z
Added getNodeForMarkdown(db, elementId, cache?): a single Cypher query (CYPHER 25, one elementId-bound MATCH plus two COLLECT { } subqueries, one per direction) returning a node's labels/properties and every relationship as raw ungrouped rows. Added a separate pure groupRelationshipsForMarkdown(rows, config) that reuses the existing getNamingStrategy/getPropertyName/sanitize/resolveCollisions logic to resolve target display names, then groups by type/direction (key = TYPE, or in_TYPE for incoming) and sorts groups alphabetically and links by target label then name.
Files: ~src/db/queries.ts, +src/db/queries.markdown.test.ts, +src/db/queries.markdown.integration.test.ts, ~.plans/tasks-markdown-mode.yml, ~AGENTS.md
Decisions: PROFILE against a live Docker Neo4j 2026.06 (4-node/4-relationship fixture, 2 types x 2 directions) showed NodeByElementIdSeek followed by two RollUpApply/Expand(All) pairs — no AllNodesScan or CartesianProduct. Collision resolution is scoped per target label across ALL of a node's relationships (not per type/direction, unlike getRelationships), since two different link types pointing at same-named same-label targets would otherwise render to the identical /Label/name.md path. Cache key reuses cacheKey.props(elementId) (node vs relationship elementIds use disjoint "4:"/"5:" prefixes, verified against a live instance, so no collision with getRelationshipProperties' cache entries). Integration test seeds/cleans up via a separate write-mode driver session since executeQuery forces READ access mode by design; it auto-skips via describe.skipIf when integration.env/env vars are absent so npm test stays green without Docker.

## TASK-004 — 2026-07-22T19:35:31Z
Added a sibling markdown-mode path parser (parseMarkdownPath) recognizing root, /index.md, /log.md, /<Label>, /<Label>/index.md, and /<Label>/<name>.md (with .md stripped), throwing LpgfsError(ENOENT) for any deeper or malformed path; classic parser/tests untouched.
Files: +src/core/markdown-path-parser.ts, +src/core/markdown-path-parser.test.ts, ~src/types/index.ts, ~.plans/tasks-markdown-mode.yml
Decisions: MarkdownPathType/MarkdownPathContext live in src/types/index.ts (re-exported from the new module) to match the codebase convention of centralizing PathContext-shaped types; unlike the classic parser's leniency, empty path segments and missing/bare .md extensions are rejected here since REQ-F-031 calls for ENOENT at the parsing stage.
## TASK-006 — 2026-07-22T21:40:00Z
Implemented propertyFallbackFieldResolver in src/markdown/fields.ts: per-field (title/timestamp/tags) ordered fallback lookup with per-label overrides, consumedProperties reporting, scalar-to-list tags coercion, and an explicit `[]` fallback list disabling a mapping.
Files: +src/markdown/fields.ts, +src/markdown/fields.test.ts, ~.plans/tasks-markdown-mode.yml
Decisions: renderer.ts's default remains passthroughFieldResolver (unchanged) — the real resolver is deliberately left unwired so it stays swappable per the Future Extensions CypherFieldResolver seam; task-007 is the intended injection point. No existing ISO-8601 serializer exists anywhere in the codebase (db/connection.ts's transformValue only decomposes Neo4j temporal values into plain objects like {year,month,day,...}), so fields.ts implements its own temporalToIso conversion from that decomposed shape (date-only/date-time/time-only, timezone offset, millisecond-truncated from nanoseconds, proleptic negative years). This format is load-bearing for task-015's byte-for-byte fixture round-trip — task-014's fixture authoring should match it rather than diverge.

## TASK-013 — 2026-07-22T22:07:00Z
Resolved the type-key clash rule: label wins the `type` frontmatter key (preserving REQ-F-021's "always includes type: <Label>" invariant), and a literal `type` property colliding with it is demoted to a `type_property` key so both values survive without silent data loss. Locked in the full frontmatter key-order contract with a dedicated exact-string ordering test.
Files: ~src/markdown/renderer.ts, ~src/markdown/renderer.test.ts, ~.plans/tasks-markdown-mode.yml
Decisions: chose "label wins, property demoted to type_property" over "property wins, label demoted to label:" because it keeps `type` always equal to the label — the stronger consistency guarantee for downstream OKF/Obsidian tooling that assumes `type` is the label. Key order is: type, type_property (only when a clash occurred), title, timestamp, tags (fixed order, not alphabetical), remaining properties (alphabetical), relationship keys (alphabetical, in_<TYPE> included). A `null`/absent `type` property is not treated as a clash. The rare double-collision case (a node with both a colliding `type` property and a genuine `type_property` property) is an accepted, documented, unhandled edge case — the demoted value overwrites it rather than being silently ambiguous.
## TASK-010 — 2026-07-22T22:20:00Z
Added listNodesForMarkdownIndex(db, label, fieldsConfig, textPropertiesConfig, config?, cache?) in src/db/queries.ts: one Cypher query per label projecting title/timestamp via COALESCE (textually ordered per the configured fallback list) plus a truncated first-present-text-property description, with display-name/collision resolution reused from getNodesByLabel. Exported the query-building helper (buildMarkdownIndexQuery) and truncateDescription as pure, independently-testable functions; exported fields.ts's toIsoTimestamp for reuse instead of duplicating temporal decoding.
Files: ~src/db/queries.ts, ~src/cache/index.ts, ~src/markdown/fields.ts, +src/db/queries.markdown-index.test.ts, +src/db/queries.markdown-index.integration.test.ts, ~.plans/tasks-markdown-mode.yml
Decisions: new cacheKey.markdownIndex(label) is deliberately prefixed "nodes:" (not a new CacheKeyPrefix) so it shares the existing label-listing TTL bucket per REQ-F-063, matching PRD guidance rather than introducing a new TTL knob. The integration test seeds under a per-run randomized label (not a fixed name) because dist/ compiled test duplicates were racing src/ copies against the one shared live container on a fixed label/marker — fixed markers are fragile under that duplication and this avoids it without touching vitest config.
## TASK-007 — 2026-07-22T22:15:00Z
Wired markdown mode into readdir/getattr/read in src/fuse/handlers.ts: each dispatches on ctx.config.mode.type === 'markdown' (checked before the classic path-parser runs) to new markdown-only helpers using parseMarkdownPath, getNodeForMarkdown, groupRelationshipsForMarkdown, and renderNodeMarkdown wired with the real propertyFallbackFieldResolver; rendered markdown is cached keyed by node elementId (new cacheKey.markdown builder) so getattr's size computation and read's content reuse the same render. Write operations needed no changes — they already threw EROFS unconditionally regardless of path/mode.
Files: ~src/fuse/handlers.ts, ~src/cache/index.ts, ~src/types/index.ts, ~src/fuse/handlers.test.ts, +src/fuse/handlers.markdown.integration.test.ts, ~.plans/tasks-markdown-mode.yml
Decisions: scoped root readdir down to label directories only for now — index.md/log.md/the classic config-file entry are omitted from the markdown-mode listing since their generation depends on task-011/task-012 (not yet implemented) and parseMarkdownPath has no PathContext for the config file; getattr/read on those path-context types (root-index/root-log/label-index) throw ENOENT. Added task-019 (deps: task-011, task-012) to close this gap once those land. groupRelationshipsForMarkdown's grouped/sorted output is flattened back into a flat link array before being handed to renderNodeMarkdown, which does its own grouping/sorting — intentional reuse of groupRelationshipsForMarkdown purely for its name-resolution/collision-handling side, not redundant work. Also fixed two pre-existing duplicate `completed_at` YAML keys (task-004, task-005) found while editing the tasks file — the file failed to parse before this fix.

## TASK-008 — 2026-07-22T21:07:20Z
Verified mode.markdown.labels filtering was already fully implemented by task-007's assertLabelAllowed (gates readdir/getattr/read on excluded labels with ENOENT) and confirmed relationship-link resolution (getNodeForMarkdown/groupRelationshipsForMarkdown/renderNodeMarkdown) never applies the allow-list, so links to excluded-label targets already render normally; closed the remaining test gaps rather than changing production code.
Files: ~src/fuse/handlers.test.ts
Decisions: no changes to src/fuse/handlers.ts, src/db/queries.ts, or src/markdown/renderer.ts were needed — this task's scope reduced to adding 4 unit tests (getattr/read ENOENT on an excluded label's node, a link to an excluded-label node still rendering as a normal Label/name link, and unset-labels rendering every label at root).

## TASK-009 — 2026-07-22T21:33:04Z
Implemented first-label-wins determinism (REQ-F-014) for multi-label nodes via a new exported chooseNodeLabel(nodeLabels, configuredLabelOrder?) in src/db/queries.ts, applied consistently at label-listing (getNodesByLabel/listNodesForMarkdownIndex), getattr/read node lookup (renderMarkdownNode/readdirMarkdownLabel in src/fuse/handlers.ts), and relationship link target resolution (groupRelationshipsForMarkdown).
Files: ~src/db/queries.ts, ~src/fuse/handlers.ts, ~src/types/index.ts, ~src/fuse/handlers.test.ts, ~src/db/queries.markdown.test.ts, ~src/db/queries.markdown-index.test.ts, ~.plans/tasks-markdown-mode.yml
Decisions: chosen label = first label in mode.markdown.labels order that the node actually carries (an explicit user-chosen ordering, wins outright even if it isn't the alphabetically-first label and even if other configured labels are absent/excluded from the node), otherwise alphabetically-first (default JS string sort — ASCII/case-sensitive, not locale-aware; fine given Neo4j labels are conventionally PascalCase). Added `labels: string[]` to NodeQueryResult and to getNodesByLabel's/buildMarkdownIndexQuery's Cypher (`labels(n) AS labels`), with a `record.labels ?? [label]` fallback so classic-mode callers/mocks lacking the column are unaffected — classic mode still duplicates multi-label nodes across every mounted label, intentionally unchanged. renderMarkdownNode reorders nodeData.labels so the chosen label is first before calling renderNodeMarkdown, since the renderer treats labels[0] as authoritative for both the `type:` frontmatter key and per-label textProperties overrides — without this, a multi-label node's `type:` could disagree with the directory it's actually mounted under. Known caveat (flagged by review, deferred as low-risk/no-current-consumer): readdirMarkdownLabel's collision resolution runs over all same-label nodes before the chosen-label filter (getNodesByLabel is shared with classic mode), while listNodesForMarkdownIndex filters by chosen label before resolving collisions — the two can disagree on a *different*, non-multi-label node's suffixed filename when a base-name collision exists against a filtered-out multi-label node. Not observable yet since only readdir/getattr/read are wired (task-011/012 still pending); task-011 should reconcile filter/collision ordering between the two paths before wiring listNodesForMarkdownIndex into index generation.

## TASK-011 — 2026-07-22T23:00:19Z
Implemented root `/index.md` (okf_version frontmatter only, one section per rendered label linking to `<Label>/index.md`) and per-label `/<Label>/index.md` (no frontmatter, bullet list of links with derived title/description) as pure renderers in src/markdown/index-renderer.ts, wired into getattr/read for the root-index/label-index PathContext types in src/fuse/handlers.ts, cached under new "nodes:"-prefixed cache keys (markdownRootIndex/markdownLabelIndex) sharing the label-listing TTL bucket.
Files: +src/markdown/index-renderer.ts, +src/markdown/index-renderer.test.ts, ~src/markdown/renderer.ts, ~src/fuse/handlers.ts, ~src/fuse/handlers.test.ts, ~src/fuse/handlers.markdown.integration.test.ts, ~src/cache/index.ts, ~src/db/queries.ts, ~src/db/queries.markdown-index.test.ts, ~.plans/tasks-markdown-mode.yml
Decisions: exported renderConceptLink/toBodyText from renderer.ts to reuse the exact link-style/scalar-formatting convention rather than duplicating it. Reconciled the filter/collision-ordering discrepancy flagged by task-009/010 — listNodesForMarkdownIndex now resolves name collisions over the full unfiltered same-label result before applying the chosen-label filter, matching readdirMarkdownLabel/getNodesByLabel, so an index.md link and the actual on-disk filename can no longer disagree for a base-name collision against a filtered-out multi-label node (regression test added). readdir listing of index.md/log.md entries themselves remains deferred to task-019 per task-007's decision, unchanged here — only getattr/read were in scope.
## TASK-012 — 2026-07-23T01:10:00Z
Implemented generation of the virtual root /log.md: date-grouped (## YYYY-MM-DD, newest first) bullets ("**Update** <link>") merged across every rendered label via listNodesForMarkdownIndex (task-010), no per-node queries. Zero-resolvable-timestamps-in-graph case renders a documented stub ("No entries have a resolvable timestamp yet.") rather than ENOENT, so /log.md stays unconditionally present with a well-defined getattr size, consistent with /index.md's contract.
Files: +src/markdown/log.ts, +src/markdown/log.test.ts, +src/fuse/handlers.markdown.log.integration.test.ts, ~src/fuse/handlers.ts, ~src/fuse/handlers.test.ts, ~src/cache/index.ts, ~.gitignore, ~.plans/tasks-markdown-mode.yml
Decisions: zero-timestamp fallback = stub file, not omission (see rationale above). cacheKey.markdownLog() deliberately reuses the "nodes:" prefix (same TTL bucket as markdownIndex/label listings, REQ-F-063). Extracted getRenderedLabels() out of readdirMarkdownRoot (no behavior change) so both readdir and /log.md generation share the mode.markdown.labels allow-list filtering logic. Root readdir still does not list index.md/log.md themselves -- unchanged, that wiring is task-019 once task-011 also lands.
Known caveat (carried from task-009, NOT fixed here): listNodesForMarkdownIndex filters to a node's chosen label before resolving naming collisions, while getNodesByLabel (which actually serves /<Label>/<name>.md) resolves collisions over every same-label node first, then filters by chosen label elsewhere. In a collision scenario where these orders disagree, a /log.md link's resolved name could point at a filename that isn't what readdir/getattr/read actually serve for that node. Reviewer flagged this as a real (if narrow) broken-link risk that task-012 newly exposes by wiring listNodesForMarkdownIndex's names into a served, user-facing link. Recommend task-011 (or a follow-up) reconcile the two ordering strategies before the generated-file set (index.md + log.md) is considered done; until then this is a known limitation, not a regression introduced by this task.

## TASK-014 — 2026-07-22T23:25:30Z
Hand-authored test/fixtures/odyssey/: 20 concept .md files across 4 labels (Character 7 incl. one multi-label Odysseus[Character,Hero], Place 5, Creature 4, Event 4), plus root index.md, log.md, and per-label index.md files, under a config that is DEFAULT_MARKDOWN_MODE_CONFIG verbatim plus an explicit labels allow-list ([Character,Place,Creature,Event]) and per-label naming.overrides.nodes.<Label>.property: 'name'.
Files: +test/fixtures/odyssey/README.md, +test/fixtures/odyssey/import.cypher, +test/fixtures/odyssey/{Character,Place,Creature,Event}/*.md, +test/fixtures/odyssey/{Character,Place,Creature,Event}/index.md, +test/fixtures/odyssey/index.md, +test/fixtures/odyssey/log.md, ~.plans/tasks-markdown-mode.yml
Decisions: rather than hand-tracing YAML/markdown output, built the 20-node/30-relationship graph as plain JS objects in a throwaway (discarded, not committed) generator script that ran `npm run build` then imported the compiled dist/markdown/{renderer,fields,index-renderer,log}.js and dist/db/queries.js (chooseNodeLabel/groupRelationshipsForMarkdown/truncateDescription) directly, mirroring exactly what src/fuse/handlers.ts's renderMarkdownNode does (chosenLabel-first label reordering, relationship row grouping) — this guarantees the fixtures byte-match production rendering rather than a hand-computed approximation, since task-015's round-trip test needs exactly that. Circe carries a literal `type` property for task-013's clash test; Eumaeus has no title/timestamp/tags source and no text property for the frontmatter-only/empty-body case; 10 of 20 nodes have a resolvable timestamp across 4 dates (one date shared by 3 nodes/2 labels) to exercise log.md's date-grouping and label-then-name tie-break sort, the other 10 are correctly absent from log.md. hone-reviewer cross-checked import.cypher against every concept file and found no discrepancies.

## TASK-015 — 2026-07-23T02:10:00Z
Added src/markdown/odyssey-fixtures.test.ts: builds the 20-node/30-relationship Odyssey graph as plain in-memory TypeScript objects (transcribed from import.cypher), runs each node through the exact production pipeline (chooseNodeLabel -> groupRelationshipsForMarkdown -> renderNodeMarkdown with propertyFallbackFieldResolver) plus renderRootIndex/renderLabelIndex/renderRootLog, and diffs the output against every fixture file (loaded via fs.readFileSync, never hardcoded) after trailing-whitespace/final-newline normalization.
Files: +src/markdown/odyssey-fixtures.test.ts, ~.plans/tasks-markdown-mode.yml
Decisions: no discrepancies found between fixtures and renderer — all 26 assertions (20 concept files, root index.md, 4 per-label index.md, log.md) passed on first run, confirming task-014's fixtures were generated correctly against the real renderer. hone-reviewer flagged two fragility points (label selection for describe.each and the fields-resolver label argument in the index-building helper both used node.labels[0]/full labels array instead of chooseNodeLabel-selected label, which happened to coincide for every fixture node but wouldn't have caught a chooseNodeLabel regression) — fixed both to key off the resolved label explicitly.
## TASK-016 — 2026-07-23T00:03:57Z
Landed as an automated Docker/neo4j-cli integration test (src/fuse/handlers.markdown.odyssey.integration.test.ts), following task-007/012's established handler-level pattern (readdir/getattr/read driven directly, no real FUSE mount) since fuse-native mounting isn't available in this sandbox; also added a documented manual full-mount procedure to test/manual.md (Test 24) for environments where FUSE actually is installed. Verified live against an ephemeral Docker Neo4j via neo4j-cli (`docker create --ephemeral`): applies test/fixtures/odyssey/import.cypher verbatim through the driver, then asserts every concept .md, root/per-label index.md, and root log.md the handlers render matches its fixture file byte-for-byte (31 tests, all passing against the live container, auto-skipping via describe.skipIf when no DB is configured).
Files: +src/fuse/handlers.markdown.odyssey.integration.test.ts, ~test/fixtures/odyssey/import.cypher, ~test/manual.md, ~vitest.config.ts, ~AGENTS.md, ~.plans/tasks-markdown-mode.yml
Decisions: running the test against a live container surfaced a real pre-existing bug in test/fixtures/odyssey/import.cypher (from task-014): three apostrophes were escaped SQL-style (`''`) instead of Cypher's backslash style (`\'`), which this Neo4j version's parser rejects outright — fixed the escape syntax only, no change to the underlying string content (verified against the corresponding rendered .md fixtures, which already contain plain apostrophes). Reviewer flagged that this suite's beforeAll/afterAll do an unscoped full-database wipe (import.cypher itself starts with `MATCH (n) DETACH DELETE n`), unlike task-007/010/012's marker-property or randomized-label-prefix scoping, which is a real race risk if integration suites run in parallel workers against one shared Neo4j container — fixed structurally by setting `fileParallelism: false` in vitest.config.ts (documented in AGENTS.md) rather than adding ad-hoc scoping to this one suite, since any future full-wipe-based integration test would have the same problem. Root readdir/label readdir still don't list index.md/log.md (task-019, unchanged) — this task's automated coverage reaches those two files via direct getattr/read rather than readdir-based listing parity; the manual procedure documents this gap explicitly.
